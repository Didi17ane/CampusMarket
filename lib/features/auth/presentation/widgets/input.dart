import 'package:flutter/material.dart';

class Input extends StatelessWidget {
  const Input({super.key, required this.label, required this.inputType});

  final String label;
  final bool inputType;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
